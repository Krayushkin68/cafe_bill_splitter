import { createStore } from 'vuex'
import {AuthModule} from "@/store/modules/auth";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    AuthModule
  }
})
